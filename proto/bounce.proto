syntax = "proto3";
package bounce;

service Signer { rpc Sign(SignRequest) returns (SignResponse); }

message SignRequest { bytes message = 1; }

message SignResponse {
  bytes pubkey = 1;
  bytes signature = 2;
}
